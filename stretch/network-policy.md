# NetworkPolicy exercise

Start with default-deny policies only after confirming the CNI enforces NetworkPolicy. Allow DNS, ingress to frontend, frontend to backend, backend to Postgres/Redis, and monitoring scraping. Verify each allowed path and one denied path.

